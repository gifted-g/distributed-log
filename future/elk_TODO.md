# ELK Integration – TODO

- Install Filebeat on the central server.
- Prospector path: `/var/log/remote/*/*.log`
- Output to Elasticsearch.
- Kibana dashboards:
  - SSH auth failures over time.
  - Top attacking IPs.
  - Geo-IP enrichment (Logstash).
